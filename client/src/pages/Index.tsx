// This file is now unused as we use the new Home component
// Redirecting to Home component for consistency

import { Home } from "./Home";

const Index = () => {
  return <Home />;
};

export default Index;
